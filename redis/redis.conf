# Redis configuration file example

# Set the port
port 6379

# Bind to all network interfaces
bind 0.0.0.0

# Allow Redis to be accessed from any IP (configure security groups in AWS for security)
protected-mode no

# Enable persistence
save 900 1
save 300 10
save 60 10000

# Enable AOF persistence
appendonly yes

